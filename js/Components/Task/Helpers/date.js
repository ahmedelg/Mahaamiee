// Define the current date
function defineCurrDate() {
  let date = new Date();
  // Organize the current date
  return organizeDate(date);
}

// Organize the current date
function organizeCurrDate(date) {
  let organizedDate = date.toString();
  return organizedDate;
}

